all : diaporama

diaporama :
		pdflatex diaporama.tex>/dev/null  && pdflatex diaporama.tex>/dev/null  

clean :
	rm -f ./*.aux
	rm -f ./*.log
	rm -f ./*.out
	rm -f ./*.pdf
	rm -f ./*.synctex.gz
	rm -f ./*.toc
	rm -f ./*.snm
	rm -f ./*.nav
